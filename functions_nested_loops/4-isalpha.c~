#include "main.h"

/**
 *
 *isalpha - function checks if alpha or number
 *
 *
 */

int _isalpha(int c)
{
	if (c <= 90 && c >= 65)
	{
	return _putchar(1);
	}
	else if (c <= 122 && c >= 97)
	{
	return _putchar(1);
	}
	else
	{
	return _putchar(0);
	}
	putchar(10);
}
