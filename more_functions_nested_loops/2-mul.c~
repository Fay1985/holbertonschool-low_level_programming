#include "main.h"
#include <stdio.h>

/*
* mul - Multiplies two integers.
*
* @a: the first integers.
* @b: the second integer.
*
* Return: it return the production of the calculation.
*/

int mul(int a, int b)
{
	return (a * b);
}
