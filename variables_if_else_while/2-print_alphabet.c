#include <stdio.h>

/**
 *
 *main - entry point
 *
 *for loop- loops over the alphabet from a to z
 */

int main(void)
{
char c;
for (c = 'a'; c <= 'z'; c++)
putchar('c');
putchar('\n');
return (0);
}
